import React from 'react'
import ReactDOM from 'react-dom/client'
import MainMenuData from './views/mainmenu/MainMenuData'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
      <MainMenuData/>
    </React.StrictMode>,
)
